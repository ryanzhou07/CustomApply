# Supabase

This is the local Supabase project boundary. Migrations, row-level security policies,
and seed data are intentionally empty until product features are implemented.

Use `npm run supabase:start` to start the local Supabase stack with Docker.

